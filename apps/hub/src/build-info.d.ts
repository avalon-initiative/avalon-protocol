// Injected by vite.config.ts's `define` — the git tag a release was built
// from, or a short commit hash for dev/CI builds. See AppFooter in HubShell.vue.
declare const __BUILD_REVISION__: string
declare const __BUILD_IS_RELEASE__: boolean

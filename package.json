{
  "name": "avalon-protocol-workspace",
  "private": true,
  "description": "JS/TS workspace root for Avalon's clients: the hub web app, the Tauri-based mobile-hub, and the shared Vue3 UI package they both depend on. The protocol/server implementation lives in the Rust Cargo workspace at the repo root, not here.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "eslint": "^9.15.0",
    "@eslint/js": "^9.15.0",
    "typescript-eslint": "^8.15.0",
    "eslint-plugin-vue": "^9.30.0",
    "globals": "^15.12.0"
  }
}

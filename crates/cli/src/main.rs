//! `avalon` — local dev/ops CLI.
//!
//! Intended commands (per PROMPT.md §7's milestone-1 vertical slice):
//! `avalon register-game`, `avalon issue-achievement`, `avalon inspect-ledger`.
//! Not yet wired to a real `avalon-server` — scaffolding only.

#[tokio::main]
async fn main() {
    println!("avalon-cli: scaffold only, no commands wired up yet");
}
